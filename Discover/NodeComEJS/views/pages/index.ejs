  <!DOCTYPE html>
  <html lang="pt-br">
  <%- include('../partials/head'); %>
  <body class="container d-flex flex-column">
    <%- include('../partials/header', {pagina: 'home'}); %>
    <main>
      <div class="jumbotron main-bg rounded border">
        <h1>Node com EJS</h1>
        <p>Vamos aprender a trabalhar com arquivos EJS</p>
      </div>
      <ul>
        <% boni.forEach(function(boniLetra){ %>
          <li>
            <strong><%= boniLetra.title %></strong> <%= boniLetra.message %>
          </li>
        <% }) %>
      </ul>
    </main>
    <%- include('../partials/footer'); %>
  </body>
  </html>